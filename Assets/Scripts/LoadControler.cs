using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadControler : MonoBehaviour
{
    // Start is called before the first frame update
    void StartGame()
    {
        Application.LoadLevel("SampleScene");   
    }
}
